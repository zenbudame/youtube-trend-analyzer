# 動作確認結果

実施日：2026-09-19

## 結果

- 自動テスト：34件成功（`python -m unittest -v`、終了コード0）。
- Streamlit AppTestによる5タブの操作確認：成功。
- v2でのローカルのStreamlitサーバー起動：成功（v2.1は上記AppTestで確認）。`/_stcore/health` が `ok` を返すことを確認。
- 依存パッケージの整合性：`pip check` 成功。
- Python構文チェック：成功。

## 確認内容

- 101個の固有チャンネルID＋重複IDを、50・50・1件の3回にまとめて取得。
- 複数ゲームに同一チャンネルが現れても、一括取得ではチャンネルAPIを1回だけ実行。
- 登録者数プリセットの全境界、任意入力の上下限、総再生数・動画本数、登録者比・最低再生数・形式の複合条件。
- 非公開・登録者数欠落・チャンネル応答欠落・公開0人と、CSVでの欠損値保持。
- 比率がすべて不明でも既存トレンドスコアを算出。
- 動画検索のページングとID重複除去。
- 人気動画の応答を再利用し、動画詳細を重複取得しないこと。
- API接続エラーや403エラーの扱いと、メッセージにキーを出さないこと。
- キャッシュによる同一リクエストの再利用、別キーの分離。
- 人気動画・キーワード分析・ゲーム比較・狙い目ランキング・長期分析を模擬応答で実行。
- 全5タブの空結果、非公開のみの結果、条件変更時の自動取得なし・旧結果の非表示、最小値が最大値を超える入力のエラー表示。
- キー未設定時の実行無効化と、保存済みキーを入力欄に表示しないこと。

## v2.1の検証

旧analyzerモジュールに2引数のadd_channel_statsを読み込んだ状態でも、v2.1のキーワード検索が新しい処理を使い、指定2件を返すことをStreamlit AppTestで確認しました。

## 今回追加した検証

- 条件外の1ページ目を飛ばして後続ページから指定件数を取得。
- 最終ページの余分な一致動画を切り詰めて正確な指定件数を表示。
- 0件のページでも次ページがあれば継続、重複動画の除外、同じページトークンによる無限ループ防止。
- 探索上限と検索結果終了を区別し、APIエラー時も前ページまでの結果を保持。
- 複数ゲームの交互探索、全対象共通の上限、チャンネル情報の再利用。
- 日本語などの検索パラメーター、登録言語照合、未登録の明示的な許可、地域別言語コード、音声言語の優先。
- 180秒の境界、Shorts候補の検索パラメーター、3〜20分の長尺動画の保持。
- 人気一覧のページ送りと言語確認、動画詳細の不要な再取得なし。
- 画面で指定件数取得・言語選択・形式別の一覧表示・条件変更時の旧結果非表示。

## 検証環境

- Windows / Python 3.14.7
- Streamlit 1.64.0 / pandas 2.3.3 / requests 2.34.2
- requirements.txtは互換範囲を指定。その他のバージョンの組み合わせは未検証。

## 未実施の確認

YouTube APIキーは提供されていないため、実際のYouTube API接続・実データでの検索・クォータ消費は確認していません。APIの統計フィールドと一括取得仕様はGoogle公式リファレンスで確認し、テストでは模擬応答を使用しています。

Streamlit Community Cloudへの実際のデプロイは行っていません。ローカル起動とアプリ内部の画面操作を検証したもので、公開先での接続確認やブラウザの見た目の目視検査は含みません。アップロード方法とSecretsの設定はREADMEを参照してください。
